import 'package:flutter/material.dart';

class SoftSkillsBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(children: [
      Text("languages, leadership, project planing, problem solving, etc"),
    ]);
  }
}
